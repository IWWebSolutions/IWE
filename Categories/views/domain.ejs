<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= domain.name %></title>
</head>
<body>
  <h1><%= domain.name %></h1>
  <ul>
    <% niche.forEach(niche => { %>
      <li>
        <a href="/niche/<%= niche.slug %>"><%= niche.name %></a>
      </li>
    <% }) %>
  </ul>
</body>
</html>